{
  "name": "webrtc-gateway-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node-pg-migrate up --migrations-dir src/migrations --envs DATABASE_URL",
    "migrate:down": "node-pg-migrate down --migrations-dir src/migrations --envs DATABASE_URL"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/jwt": "^9.0.0",
    "@fastify/websocket": "^11.0.0",
    "bcryptjs": "^2.4.3",
    "fastify-plugin": "^5.0.0",
    "node-pg-migrate": "^7.0.0",
    "pg": "^8.13.0"
  }
}
